Flask>=3.0
qdrant-client>=1.9.0
fastembed>=0.4.0
ollama>=0.6
PyMuPDF>=1.24
pypdf>=4.0
python-docx>=1.0
pdf2image>=1.16
pillow>=10.0
PyYAML>=6.0
watchdog>=4.0
pytest>=8.0
pytest-flask>=1.3
python-dateutil>=2.8
duckduckgo-search>=6.0
beautifulsoup4>=4.12
faster-whisper>=1.0

# Optional: V-JEPA 2.1 smart keyframe selection for video ingestion
# python scripts/setup_vjepa.py   (pip + git clone + ~300 MB model download)
# Requires: torch torchvision timm einops  (~2 GB on first install)

# Optional: text-to-video generation (requires GPU with 8GB+ VRAM)
# pip install diffusers torch transformers accelerate imageio[ffmpeg]
